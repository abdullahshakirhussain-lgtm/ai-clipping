/**
 * GENERATED — do not edit by hand.
 *
 * One base64-encoded PNG per Story style, used as a fixed STYLE REFERENCE fed to
 * gpt-image's edits endpoint on every frame so the chosen style is enforced by
 * EXAMPLE, not by a text description (which gpt-image-1-mini collapses). Regenerate
 * with:  pnpm tsx scripts/gen-style-exemplars.ts   (needs OPENAI_API_KEY).
 *
 * Empty until the script is run — with no entry for a style, the pipeline simply
 * falls back to the text style anchor for that style (old behaviour).
 */
export const STYLE_REFS: Record<string, string> = {};
