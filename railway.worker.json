{
  "$schema": "https://railway.com/railway.schema.json",
  "//": "Only needed for the SCALABLE topology (QUEUE_DRIVER=bullmq). In the simple single-service deploy the API runs the pipeline in-process and this service is not created.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "pnpm --filter @clipfactory/worker start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
